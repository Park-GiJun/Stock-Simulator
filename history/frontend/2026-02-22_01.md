# 2026-02-22_01: 프론트엔드 뉴스 API 연결

## 변경 사항

### 1. 뉴스 API 클라이언트 생성
- `newsApi.ts` 신규 생성
- `getNewsList(params)`, `getNewsById(newsId)`, `getNewsByStock(stockId)` 함수
- 엔드포인트: `/event-service/api/news`

### 2. NewsArticle 타입 추가
- `types/news.ts`에 `NewsArticle` 인터페이스 추가 (백엔드 `NewsArticleResponse` 매핑)
- 기존 `NewsEvent` (mock용) 유지

### 3. 뉴스 페이지 백엔드 연결
- mock import (`getNewsList`, `getNewsById`, `MOCK_NEWS`) 제거
- `newsApi.getNewsList()` 호출로 전환
- 필터 변경 시 `setLevel()` / `setSentiment()` 핸들러로 re-fetch
- 페이지네이션 (이전/다음) 추가
- `NewsArticle` 타입 사용 (publishedAt 기준 시간 표시)

### 4. 대시보드 뉴스 섹션 연결
- `getActiveNews()` (mock) → `newsApi.getNewsList({ size: 5 })` 교체
- `onMount`에서 다른 API와 함께 병렬 fetch
- 빈 상태 표시 추가

## 수정 파일 목록

| 파일 | 변경 |
|------|------|
| `frontend/src/lib/api/newsApi.ts` | 신규 생성 - 뉴스 API 클라이언트 |
| `frontend/src/lib/types/news.ts` | `NewsArticle` 인터페이스 추가 |
| `frontend/src/routes/(desktop)/news/+page.svelte` | mock → 백엔드 API 연결 |
| `frontend/src/routes/(desktop)/+page.svelte` | 대시보드 뉴스 mock → API |
