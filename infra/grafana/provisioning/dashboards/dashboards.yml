apiVersion: 1

providers:
  - name: 'Stock Simulator Dashboards'
    orgId: 1
    folder: 'Stock Simulator'
    folderUid: 'stocksim'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
